export interface IUser {
  id: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface CreateUserPayload {
  // TODO: Add fields
}

export interface UpdateUserPayload {
  // TODO: Add fields
}
